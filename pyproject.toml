[tool.black]
line-length = 120
target-version = ['py39']
include = '\.py$'
exclude = '''
(
    migrations
    | node_modules
    | /venv
    | /.tox
)
'''

[tool.isort]
line_length = 120
multi_line_output = 3
include_trailing_comma = true
skip_glob = "*/migrations/*.py, /venv"
